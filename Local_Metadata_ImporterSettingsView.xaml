<UserControl x:Class="Local_Metadata_Importer_plugin.Local_Metadata_ImporterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Local_Metadata_Importer_plugin"
             mc:Ignorable="d" d:DesignWidth="688" d:DesignHeight="526.333"
>

    <ScrollViewer Name="ScrollViewer" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
        <StackPanel Margin="5">
            <GroupBox Margin="0,0,0,0">
                <GroupBox.Header>
                    <Label FontWeight="Bold" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_GenericSettings}"/>
                </GroupBox.Header>
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <CheckBox Name="ChkGameNameInsteadRomName" Margin="0,0,5,0" IsChecked="{Binding Settings.SearchGameNameFirst}" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_Match}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>
            <GroupBox Margin="0,5,0,0">
                <GroupBox.Header>
                    <Label FontWeight="Bold" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_PlatformSettings}"/>
                </GroupBox.Header>
                <StackPanel>
                    <DataGrid Margin="0,0,0,0" SelectionMode="Single" x:Name="PagesDataGrid" Height="220" AutoGenerateColumns="False" 
                              CanUserSortColumns="False" Loaded="PagesDataGrid_Loaded" EnableRowVirtualization="False" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColPlatform}" Binding="{Binding Platform}" Width="150" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="ToolTip" Value="{Binding Platform}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColIcons}" Binding="{Binding IconFolderName}" Width="*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="ToolTip" Value="{Binding IconFolderName}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColCovers}" Binding="{Binding CoverFolderName}" Width="*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="ToolTip" Value="{Binding CoverFolderName}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColBackgrounds}" Binding="{Binding BackgroundFolderName}" Width="*"  IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="ToolTip" Value="{Binding BackgroundFolderName}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColLogos}" Binding="{Binding LogoFolderName}" Width="*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="ToolTip" Value="{Binding LogoFolderName}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColVideos}" Binding="{Binding VideoFolderName}" Width="*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="ToolTip" Value="{Binding VideoFolderName}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <StackPanel Margin="0,5,0,0">
                        <Grid x:Name="GridPanel" Margin="0">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="190"/>
                                <ColumnDefinition Width="8*"/>
                                <ColumnDefinition Width="110"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Row="0" Grid.Column="0" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColPlatform}"/>
                            <ComboBox Grid.Row="0" Grid.Column="1" Name="PlatformCombobox" Height="23" ItemsSource="{Binding AvailablePlatforms}"  
                                SelectedValue="{Binding Path=SelectedItem.Platform, ElementName=PagesDataGrid}" VerticalAlignment="Center" 
                                HorizontalAlignment="Stretch" Margin="0,5,0,0">
                            </ComboBox>

                            <Label Grid.Row="1" Grid.Column="0" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColIcons}"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Height="23" Text="{Binding Path=SelectedItem.IconFolderName, ElementName=PagesDataGrid}" Name="TbIconFolderName" HorizontalContentAlignment="Stretch"></TextBox>
                            <Button Grid.Row="1" Grid.Column="2" x:Name="ButSelectIconFolder" DockPanel.Dock="Right" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_Select}" Width="105" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Click="ButSelectIconFolder_Click"/>

                            <Label Grid.Row="2" Grid.Column="0" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColCovers}" />
                            <TextBox Grid.Row="2" Grid.Column="1" Height="23" Text="{Binding Path=SelectedItem.CoverFolderName, ElementName=PagesDataGrid}" Name="TbCoverFolderName" HorizontalContentAlignment="Stretch"></TextBox>
                            <Button Grid.Row="2" Grid.Column="2" x:Name="ButSelectCoverFolder" DockPanel.Dock="Right" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_Select}" Width="105" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Click="ButSelectCoverFolder_Click"/>

                            <Label Grid.Row="3" Grid.Column="0" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColBackgrounds}" />
                            <TextBox Grid.Row="3" Grid.Column="1" Height="23" Text="{Binding Path=SelectedItem.BackgroundFolderName, ElementName=PagesDataGrid}" Name="TbBackgroundFolderName" HorizontalContentAlignment="Stretch"></TextBox>
                            <Button Grid.Row="3" Grid.Column="2" x:Name="ButSelectBackgroundFolder" DockPanel.Dock="Right" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_Select}" Width="105" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Click="ButSelectBackgroundFolder_Click"/>
                            
                            <Label Grid.Row="4" Grid.Column="0" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColLogos}" />
                            <TextBox Grid.Row="4" Grid.Column="1" Height="23" Text="{Binding Path=SelectedItem.LogoFolderName, ElementName=PagesDataGrid}" Name="TbLogoFolderName" HorizontalContentAlignment="Stretch" ></TextBox>
                            <Button Grid.Row="4" Grid.Column="2" x:Name="ButSelecLogoFolder" DockPanel.Dock="Right" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_Select}" Width="105" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Click="ButSelectLogoFolder_Click"/>

                            <Label Grid.Row="5" Grid.Column="0" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_ColVideos}" />
                            <TextBox Grid.Row="5" Grid.Column="1" Height="23" Text="{Binding Path=SelectedItem.VideoFolderName, ElementName=PagesDataGrid}" Name="TbVideoFolderName" HorizontalContentAlignment="Stretch"></TextBox>
                            <Button Grid.Row="5" Grid.Column="2"  x:Name="ButSelecVideoFolder" DockPanel.Dock="Right" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_Select}" Width="105" HorizontalContentAlignment="Center" HorizontalAlignment="Right" Click="ButSelectVideoFolder_Click"/>
                        </Grid>
                    </StackPanel>
                        
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <Button Name="BtnAdd" Grid.Column="0" Margin="4,0,4,0" Click="BtnAdd_Click" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_ButAddPage}"></Button>
                        <Button Name="BtnDel" Grid.Column="1" Margin="4,0,4,0" Click="BtnDel_Click" Content="{DynamicResource LOC_LOCALMETADATAIMPORTER_ButDelPage}"></Button>
                    </Grid>
                </StackPanel>

            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>